<!DOCTYPE html>
<html>
<head>
	<title>The Calisthenics Bible</title>
	<link rel='stylesheet' type = 'text/css' href = 'https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.10/semantic.css'>
	<link rel='stylesheet' type = 'text/css' href = '/stylesheets/app.css'>
	<link rel="shortcut icon" href="/favicon2.ico" />
</head>
<body>
	<div class='ui fixed inverted menu'>
		<div class='ui container'>
			<div class='header item'><i class='male icon'></i>Calisthenics</div>
			<a class = 'item' href='/'>Home</a>
			<a class = 'item' href='/calisthenics/new'>New Move</a>
		</div>
		<div class='right menu'>
			<% if(!currentUser){ %>
			<a href="/login" class='item'>Login</a>
			<a href="/register" class='item'>Sign Up</a>
			<% } else { %>
				<a class='item' href="#">Signed In As <%= currentUser.username %></a>
                <a class = 'item' href="/logout">Logout</a>
                <% } %>
		</div>
	</div>

<% if( (error && error.length>0)  || (success && success.length >0 )){%>
<div id='flashContainer' class="ui container">
        <% if(error && error.length > 0){ %>
            <div class="ui warning message" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="ui warning message" role="alert">
                <%= success %>
            </div>
        <% } %>
</div>
<%}%>
